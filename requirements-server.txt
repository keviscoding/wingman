# Server-only deps. No macOS-specific (mss/pyobjc/pynput/pyaudio) so this
# runs cleanly on any Linux container (DigitalOcean, Railway, Fly).
google-genai>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
python-dotenv>=1.0.0
Pillow>=11.0.0
thefuzz>=0.22.0
python-Levenshtein>=0.26.0
python-multipart>=0.0.18
# Auth (saas mode)
bcrypt>=4.2.0
python-jose[cryptography]>=3.3.0
email-validator>=2.2.0
# Optional: KIE / Atlas Cloud / Grok HTTP calls
httpx>=0.27.0
# Postgres (managed prod DB). Falls back to stdlib sqlite3 locally
# when DATABASE_URL isn't set.
psycopg[binary]>=3.2.0
