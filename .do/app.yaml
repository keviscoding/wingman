name: wingman
services:
  - name: wingman
    dockerfile_path: Dockerfile
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    envs:
      - key: WINGMAN_HEADLESS
        value: "1"
      - key: GEMINI_API_KEY
        type: SECRET
    routes:
      - path: /
